{
    "version": 2,
    "buildCommand": "echo 'Static site - no build needed'",
    "outputDirectory": "public",
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        }
    ]
}
